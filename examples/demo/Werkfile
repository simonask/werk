default cache-dir = "../../target/examples/demo"

let cc = which "clang"
let ld = cc

build "target/%.o" {
    run "{cc} -c <in> -o <out> -MF <depfile> -MM -MT <in>"
}

task build {
    build "target/program{EXE_SUFFIX}"
}
